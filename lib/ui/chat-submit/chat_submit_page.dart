import 'package:flutter/material.dart';

class ChatSubmitPage extends StatelessWidget {
  const ChatSubmitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("ChatSubmitPage");
  }
}
