import 'package:flutter/material.dart';

class ChatUpdateCategory extends StatelessWidget {
  const ChatUpdateCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
